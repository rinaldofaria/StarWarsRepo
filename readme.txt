#comandos para uso em configuração.

#para renomear um repositório (se foi renomeado no GitHub, por exemplo, depois de ter sido clonado)
git remote set-url origin <new-url> --exemplo: https://github.com/rinaldofaria/StarWarsRepo.git

#para listar a configuração de um repositório
git config -l
